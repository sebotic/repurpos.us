import { StandardizeAssayTypePipe } from './standardize-assay-type.pipe';

describe('StandardizeAssayTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StandardizeAssayTypePipe();
    expect(pipe).toBeTruthy();
  });
});
