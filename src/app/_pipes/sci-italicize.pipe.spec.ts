import { SciItalicizePipe } from './sci-italicize.pipe';

describe('SciItalicizePipe', () => {
  it('create an instance', () => {
    const pipe = new SciItalicizePipe();
    expect(pipe).toBeTruthy();
  });
});
